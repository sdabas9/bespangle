cmake_minimum_required(VERSION 3.10)
project(bounties)
set(ORG_CONTRACT ${ORG_CONTRACT})
set(ANDEMITTER_CONTRACT ${ANDEMITTER_CONTRACT})
set(REQUESTS_CONTRACT ${REQUESTS_CONTRACT})
set(AUTHORITY_CONTRACT ${AUTHORITY_CONTRACT})
set(STATISTICS_CONTRACT ${STATISTICS_CONTRACT})
set(CUMULATIVE_CONTRACT ${CUMULATIVE_CONTRACT})
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/andemitter.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/andemitter.hpp @ONLY)
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/org.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/org.hpp @ONLY)
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/authority.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/authority.hpp @ONLY)
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/bounties.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/bounties.hpp @ONLY)
