cmake_minimum_required(VERSION 3.10)
project(simnotify)
set(SIMPLE_INTERFACE_CONTRACT_NAME ${SIMPLE_INTERFACE_CONTRACT_NAME})
set(ANDEMITTER_CONTRACT_NAME ${ANDEMITTER_CONTRACT_NAME})
set(SIMPLEBADGE_CONTRACT_NAME ${SIMPLEBADGE_CONTRACT_NAME})
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/simnotify.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/simnotify.hpp @ONLY)
