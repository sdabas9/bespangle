cmake_minimum_required(VERSION 3.10)
project(abmanager)
set(ORG_CHECKS_CONTRACT_NAME ${ORG_CHECKS_CONTRACT})
set(ANTIBADGE_CONTRACT ${ANTIBADGE_CONTRACT})
set(ORCHESTRATOR_CONTRACT ${ORCHESTRATOR_CONTRACT})
set(ANTIBADGE_VALIDATION_CONTRACT ${ANTIBADGE_VALIDATION_CONTRACT})

configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/abmanager.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/abmanager.hpp @ONLY)
