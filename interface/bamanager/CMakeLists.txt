cmake_minimum_required(VERSION 3.10)
project(bamanager)
set(ORG_CONTRACT ${ORG_CONTRACT})
set(BOUNDED_AGG_CONTRACT ${BOUNDED_AGG_CONTRACT})
set(ORCHESTRATOR_CONTRACT ${ORCHESTRATOR_CONTRACT})
set(BOUNDED_AGG_VALIDATION_CONTRACT ${BOUNDED_AGG_VALIDATION_CONTRACT})

configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/bamanager.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/bamanager.hpp @ONLY)
