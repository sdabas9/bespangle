cmake_minimum_required(VERSION 3.10)
project(grmanager)
set(ORG_CONTRACT ${ORG_CONTRACT})
set(GIVER_REP_VALIDATION_CONTRACT ${GIVER_REP_VALIDATION_CONTRACT})
set(GIVER_REP_CONTRACT ${GIVER_REP_CONTRACT})
set(ORCHESTRATOR_CONTRACT ${ORCHESTRATOR_CONTRACT})
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/grmanager.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/grmanager.hpp @ONLY)
