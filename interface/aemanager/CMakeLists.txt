cmake_minimum_required(VERSION 3.10)
project(aemanager)
set(ORG_CONTRACT ${ORG_CONTRACT})
set(ANDEMITTER_VALIDATION_CONTRACT ${ANDEMITTER_VALIDATION_CONTRACT})
set(ANDEMITTER_CONTRACT ${ANDEMITTER_CONTRACT})
set(ORCHESTRATOR_CONTRACT ${ORCHESTRATOR_CONTRACT})
set(SIMPLEBADGE_CONTRACT ${SIMPLEBADGE_CONTRACT})

configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/aemanager.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/aemanager.hpp @ONLY)
