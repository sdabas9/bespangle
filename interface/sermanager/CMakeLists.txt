cmake_minimum_required(VERSION 3.10)
project(sermanager)
set(ORG_CHECKS_CONTRACT_NAME ${ORG_CHECKS_CONTRACT})
set(SERIESBADGE_CONTRACT ${SERIESBADGE_CONTRACT})
set(ORCHESTRATOR_CONTRACT ${ORCHESTRATOR_CONTRACT})
set(SERIES_VALIDATION_CONTRACT ${SERIES_VALIDATION_CONTRACT})

configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/sermanager.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/sermanager.hpp @ONLY)
