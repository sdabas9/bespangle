cmake_minimum_required(VERSION 3.10)
project(simmanager)
set(ORG_CHECKS_CONTRACT_NAME ${ORG_CHECKS_CONTRACT})
set(SIMPLE_NOTIFICATION_CONTRACT ${SIMPLE_NOTIFICATION_CONTRACT})
set(NOTIFICATION_CONTRACT ${NOTIFICATION_CONTRACT})
set(SIMPLE_VALIDATION_CONTRACT ${SIMPLE_VALIDATION_CONTRACT})
set(ORCHESTRATOR_CONTRACT ${ORCHESTRATOR_CONTRACT})

configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/simmanager.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/simmanager.hpp @ONLY)
