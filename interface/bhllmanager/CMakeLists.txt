cmake_minimum_required(VERSION 3.10)
project(bhllmanager)
set(ORG_CONTRACT ${ORG_CONTRACT})
set(BOUNDED_HLL_VALIDATION_CONTRACT ${BOUNDED_HLL_VALIDATION_CONTRACT})
set(BOUNDED_HLL_CONTRACT ${BOUNDED_HLL_CONTRACT})
set(ORCHESTRATOR_CONTRACT ${ORCHESTRATOR_CONTRACT})
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/bhllmanager.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/bhllmanager.hpp @ONLY)
