cmake_minimum_required(VERSION 3.10)
project(antibadge)
set(AUTHORITY_CONTRACT ${AUTHORITY_CONTRACT})
set(ORCHESTRATOR_CONTRACT ${ORCHESTRATOR_CONTRACT})
set(BOUNDED_AGG_CONTRACT ${BOUNDED_AGG_CONTRACT})
set(CUMULATIVE_CONTRACT ${CUMULATIVE_CONTRACT})
set(ORG_CONTRACT ${ORG_CONTRACT})

configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/antibadge.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/antibadge.hpp @ONLY)
