cmake_minimum_required(VERSION 3.10)
project(cumulative)
set(ORCHESTRATOR_CONTRACT ${ORCHESTRATOR_CONTRACT})
set(BILLING_CONTRACT ${BILLING_CONTRACT})
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/cumulative.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/cumulative.hpp @ONLY)
