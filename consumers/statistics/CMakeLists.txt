cmake_minimum_required(VERSION 3.10)
project(statistics)
set(CUMULATIVE_CONTRACT ${CUMULATIVE_CONTRACT})
set(ORCHESTRATOR_CONTRACT ${ORCHESTRATOR_CONTRACT})
set(AUTHORITY_CONTRACT ${AUTHORITY_CONTRACT})
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/statistics.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/statistics.hpp @ONLY)
