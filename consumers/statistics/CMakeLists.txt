cmake_minimum_required(VERSION 3.10)
project(statistics)
set(CUMULATIVE_CONTRACT_NAME ${CUMULATIVE_CONTRACT_NAME})
set(ORCHESTRATOR_CONTRACT_NAME ${ORCHESTRATOR_CONTRACT_NAME})
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/statistics.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/statistics.hpp @ONLY)
