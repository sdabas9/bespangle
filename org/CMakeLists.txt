cmake_minimum_required(VERSION 3.10)
project(org)
set(SIMPLEBADGE_CONTRACT ${SIMPLEBADGE_CONTRACT})
set(GOTCHABADGE_CONTRACT ${GOTCHABADGE_CONTRACT})
set(ORCHESTRATOR_CONTRACT ${ORCHESTRATOR_CONTRACT})
set(AA_BADGE_CONTRACT ${AA_BADGE_CONTRACT})
set(CUMULATIVE_CONTRACT ${CUMULATIVE_CONTRACT})
set(ROUNDS_CONTRACT ${ROUNDS_CONTRACT})
set(ACCOUNT_PREFERENCES_CONTRACT ${ACCOUNT_PREFERENCES_CONTRACT})
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/include/org.hpp.in ${CMAKE_CURRENT_BINARY_DIR}/include/org.hpp @ONLY)
